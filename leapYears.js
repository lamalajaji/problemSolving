function isLeapYear(year) {
  // TODO
  if (year % 4 === 0) {
    if (year % 400 === 0 && year % 100 !== 0) {
    }
    return true;
  } else {
    return false;
  }
}


///// actully i thought this problem will be complicated but i solve it in first test :)